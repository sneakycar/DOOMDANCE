# Assets

## In-game (pixel lock)

`pixel/` — only folder referenced by scenes. Regenerate with `tools/convert_pixel_art.sh`.

## Deprecated for shipping

- `kensington_alley_bg.png` — painted source; do not assign in scenes
- `player/` — legacy hi-res sprites; superseded by `pixel/`

Reference art may remain in `generated images/`.
